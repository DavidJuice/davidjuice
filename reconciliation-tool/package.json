{
  "name": "ab-reconciliation-tool",
  "version": "0.1.0",
  "private": true,
  "description": "HIPAA-scope AgencyBloc reconciliation tool (Google Apps Script + Sheets)",
  "scripts": {
    "build": "bash scripts/build.sh",
    "push": "npm run build && clasp push",
    "open": "clasp open",
    "logs": "clasp logs --watch"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
